git init => for initializing git repository

git status=>list all untracked files 

git add -A => adding all untracked files to sa

git commit -m "commit message"=>sa => LR

git config --global user.name "uername"

git config --global user.email "email"

git remote add remote_name remote_path (for adding remote)

git push remote_name branch_name(master/main)

git branch

git checkout 